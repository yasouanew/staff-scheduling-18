import * as AlertDialog from '@radix-ui/react-alert-dialog';
import { RotateCcw } from 'lucide-react';
import type { BillingPayment } from '@/types/billing';
export function RefundButton({payment,isPending,onConfirm}:{payment:BillingPayment;isPending?:boolean;onConfirm:()=>Promise<void>}):JSX.Element{return <AlertDialog.Root><AlertDialog.Trigger asChild><button disabled={isPending} className="inline-flex h-9 items-center gap-1 rounded-lg border border-danger/30 px-3 text-sm font-semibold text-danger hover:bg-danger/10 disabled:opacity-60"><RotateCcw className="h-4 w-4"/>Refund</button></AlertDialog.Trigger><AlertDialog.Portal><AlertDialog.Overlay className="fixed inset-0 z-50 bg-foreground/40"/><AlertDialog.Content className="fixed left-1/2 top-1/2 z-50 w-[calc(100%-2rem)] max-w-md -translate-x-1/2 -translate-y-1/2 rounded-xl border border-border bg-card p-6 shadow-xl"><AlertDialog.Title className="text-lg font-semibold text-foreground">Confirm full refund</AlertDialog.Title><AlertDialog.Description className="mt-2 text-sm text-muted-foreground">This refunds the remaining refundable amount for this Stripe payment. The action cannot be automatically undone.</AlertDialog.Description><div className="mt-6 flex justify-end gap-3"><AlertDialog.Cancel className="h-10 rounded-lg border border-input px-4 text-sm font-medium text-foreground">Keep payment</AlertDialog.Cancel><AlertDialog.Action disabled={isPending} onClick={()=>void onConfirm()} className="h-10 rounded-lg bg-danger px-4 text-sm font-semibold text-danger-foreground disabled:opacity-60">{isPending?'Refunding…':'Confirm refund'}</AlertDialog.Action></div></AlertDialog.Content></AlertDialog.Portal></AlertDialog.Root>}
