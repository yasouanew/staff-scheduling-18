import { format, parseISO } from 'date-fns';
import type { BillingPayment } from '@/types/billing';
import { RefundButton } from './RefundButton';
const money=(amount:number,currency:string)=>new Intl.NumberFormat('en-AU',{style:'currency',currency}).format(amount);
export function PaymentsTable({payments,isRefunding,onRefund}:{payments:BillingPayment[];isRefunding?:boolean;onRefund:(payment:BillingPayment)=>Promise<void>}):JSX.Element{return <div className="overflow-x-auto rounded-xl border border-border bg-card"><table className="w-full text-left text-sm"><thead className="bg-muted/40 text-xs uppercase text-muted-foreground"><tr><th className="px-4 py-3">Date</th><th className="px-4 py-3">Amount</th><th className="px-4 py-3">Provider</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Refunded</th><th className="px-4 py-3"></th></tr></thead><tbody>{payments.map(payment=><tr key={payment.id} className="border-t border-border"><td className="px-4 py-4 text-foreground">{payment.paidAt?format(parseISO(payment.paidAt),'d MMM yyyy'):'—'}</td><td className="px-4 py-4 text-foreground">{money(payment.amount,payment.currency)}</td><td className="px-4 py-4 capitalize text-muted-foreground">{payment.provider}</td><td className="px-4 py-4"><span className="rounded-full bg-muted px-2.5 py-1 text-xs font-semibold text-foreground">{payment.status}</span></td><td className="px-4 py-4 text-muted-foreground">{payment.amountRefunded?money(payment.amountRefunded,payment.currency):'—'}</td><td className="px-4 py-4">{payment.isRefundable?<RefundButton payment={payment} isPending={isRefunding} onConfirm={()=>onRefund(payment)}/>:null}</td></tr>)}</tbody></table></div>}
