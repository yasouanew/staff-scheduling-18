import { Edit3 } from 'lucide-react';
import type { BillingPlan } from '@/types/billing';

const money=(value:number)=>new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD'}).format(value);
export function PlansTable({plans,onEdit,onDelete}:{plans:BillingPlan[];onEdit:(plan:BillingPlan)=>void;onDelete:(plan:BillingPlan)=>void}):JSX.Element{return <div className="overflow-x-auto rounded-xl border border-border bg-card shadow-sm"><table className="w-full text-left text-sm"><thead className="border-b border-border bg-muted/40 text-xs uppercase tracking-wide text-muted-foreground"><tr><th className="px-4 py-3">Plan</th><th className="px-4 py-3">Monthly</th><th className="px-4 py-3">6 months</th><th className="px-4 py-3">Yearly</th><th className="px-4 py-3">Limits</th><th className="px-4 py-3">Subscribers</th><th className="px-4 py-3">Status</th><th className="px-4 py-3"><span className="sr-only">Actions</span></th></tr></thead><tbody>{plans.map(plan=><tr key={plan.id} className="border-b border-border last:border-0"><td className="px-4 py-4"><p className="font-semibold text-foreground">{plan.name}</p><p className="text-xs text-muted-foreground">{plan.features.length} features</p></td><td className="px-4 py-4 text-foreground">{money(plan.priceMonthly)}</td><td className="px-4 py-4 text-foreground">{plan.priceSixMonthly===null?'Not configured':money(plan.priceSixMonthly)}</td><td className="px-4 py-4 text-foreground">{money(plan.priceYearly)}</td><td className="px-4 py-4 text-muted-foreground">{plan.maxEmployees??'Unlimited'} employees<br/>{plan.maxBranches??'Unlimited'} branches</td><td className="px-4 py-4 text-foreground">{plan.subscriptionsCount??'—'}</td><td className="px-4 py-4"><span className={plan.isActive?'rounded-full bg-success/10 px-2.5 py-1 text-xs font-semibold text-success':'rounded-full bg-muted px-2.5 py-1 text-xs font-semibold text-muted-foreground'}>{plan.isActive?'Active':'Inactive'}</span></td><td className="px-4 py-4"><button type="button" onClick={()=>onEdit(plan)} className="inline-flex h-9 items-center gap-1 rounded-lg border border-input px-3 font-medium text-foreground hover:bg-secondary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"><Edit3 className="h-4 w-4"/>Edit</button><button type="button" onClick={()=>onDelete(plan)} className="ml-2 inline-flex h-9 rounded-lg border border-danger/30 px-3 font-medium text-danger hover:bg-danger/10">Delete</button></td></tr>)}</tbody></table></div>}
